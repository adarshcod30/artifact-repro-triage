# Improvement Changelog

Every meaningful iteration, the evidence that prompted it, and what I decided next.
Schema follows the challenge brief.

## Corpus construction

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Baseline | Scrape ISSTA 2024 Artifact Evaluation page for expert badge labels. Needed ground truth I did not author myself. | 43 artifacts parsed via the `data-facet-badge` attribute. Distribution: Available 23, Functional 11, Reusable 9. | Kept. Labels are expert-assigned, published, and independent of this project. Parsing the data attribute (not visible text) survives theme changes. |
| Iteration 1 | Resolve each title to its Zenodo deposit and download the artifact for analysis. The deposit is literally what the committee badged. | Total corpus download would be **326 GB**. Largest single artifact 190 GB; only 6 of 21 resolved deposits were under 100 MB. | **Removed.** Downloading deposits is infeasible and would also wreck our own reproducibility - a judge cannot re-run a 326 GB pipeline. |
| Iteration 2 | 11 of 43 rows were lost during resolution. Investigated rather than accepting the loss. | All 11 failures were `HTTP 429 TOO MANY REQUESTS`, not genuine absences. | Added exponential backoff (2s doubling, 6 attempts). Resolved rows went **19 -> 28**. Lesson: a silent "not found" was really a rate limit. |
| Iteration 3 | Harvest GitHub mirrors from Zenodo metadata instead of downloading deposits. Research artifacts almost always mirror their code on GitHub. | 15 of 31 resolved records carry a GitHub repo. Badge spread 6 Reusable / 4 Available / 5 Functional - better balanced than the raw 43. | Kept. Shallow clone measured at 2.3s / 8.6 MB per repo, versus 326 GB for deposits. |

| Iteration 4 | Recover the 28 artifacts without a Zenodo repo link by searching GitHub for the paper title. | 20 recovered - but inspection found `jekyll/minima` matched to a patch-generation paper, `sleuthkit/sleuthkit` to an unrelated fuzzer named Sleuth, and `SimonHung/LodeRunner_TotalRecall` (a game) to "Total Recall? How Good are Static Call Graphs Really?". | Keyword overlap on repo *metadata* matches almost anything. Generic paper vocabulary is not a fingerprint. |
| Iteration 5 | Harden verification: require the repo's README to corroborate - verbatim title phrase, an ISSTA 2024 citation, or full keyword coverage. | False positives fell from 20 to 5, but the LodeRunner game *still* passed: its README genuinely contains "total recall". | Tightening a fuzzy matcher shrinks the error rate without eliminating the error class. |
| Iteration 6 | **Removed GitHub search entirely.** Kept only repo links the artifact authors published in their own Zenodo deposits. | Corpus: 15 artifacts, zero false positives, badges balanced 6 Reusable / 4 Available / 5 Functional. All 15 slugs verified live against the GitHub API. | Kept. Since the corpus *is* the ground truth, a wrong label is worse than a smaller N. 15 clears the brief's "ten or more cases" target. |
| Bugfix | Two repo slugs were truncated (`upbea`, `..._Artifac`). | `"upbeat".rstrip(".git")` returns `"upbea"` - `rstrip` strips any trailing characters in the set, not the suffix. | Replaced with `removesuffix`. Would have broken 2 of 15 clones with a confusing 404. |

## Scrubber (leakage defence)

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 7 | Artifacts often announce their own badge in the README. Built a scrubber so the model judges the work instead of reading the answer. | Self-test caught three bugs in my first version: redactions nested inside each other; `.../badge/artifact-reusable-green` kept the tier word after the word "badge" alone was redacted; and "results **were** reproduced" evaded a pattern expecting adjacent words. | Reordered patterns so broad structures (images, URLs, whole sentences) fire before the bare-word rule, and made the redaction token keyword-free so it cannot re-match. |
| Iteration 8 | Added a post-condition asserting no tier word survives scrubbing, rather than trusting the patterns. | All six adversarial samples now scrub clean; the assertion fails loudly if any `reusable`/`functional` survives. | Kept. A scrubber that is merely *believed* to work is worth nothing - the audit has to be executable. |

## Fact-sheet extraction

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 9 | Shallow-clone each repo to read its file tree and README. | `zhangxiaosa/LPR` alone was **15 GB**; the run drove the disk from 18 GB free to **1.9 GB (100% full)** before I killed it. Research repos routinely commit datasets, models and VM images. | **Removed.** `--depth 1` still transfers every blob at HEAD. |
| Iteration 10 | Replaced cloning with the GitHub tree API: recursive file listing plus the README endpoint. | 15/15 fact sheets built in seconds, **0 bytes of disk**, pinned to explicit commit SHAs. The same repo reports 430 MB of HEAD blobs versus 15 GB cloned - the rest was history and LFS. | Kept. Faster, safer, and deterministic. The thing I actually needed (does path X exist?) never required file contents. |
| Iteration 11 | First tree-API run timed out at 2 minutes. | Every call slept 2.2s - a throttle sized for `/search/*` (30 req/min) applied to the whole REST API (5000/hr). | Throttle now depends on endpoint. Build time went from >120s to seconds. |
| **Leakage measured** | Ran the scrubber across all 15 fact sheets to quantify the risk rather than assume it. | **4 of 15 READMEs (27%) disclosed their own ACM badge tier.** | Confirms the scrubber is load-bearing, not defensive decoration. Without it, 27% of the corpus would hand the model its own answer. |

## Claim verification (the core mechanism)

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 12 | Extract every file path a README references, then check each against the real file tree. A README is a set of promises; broken promises are hard evidence, established with zero model calls. | First run reported implausible ratios - 55 of 58 claims "broken" on one artifact. | Investigated instead of reporting it. |
| Iteration 13 | Inspected the extracted claims directly. | The extractor matched any token containing a dot: version numbers (`3.10.12`, `0.01`), Java class names (`com.baidu...EchoServiceTest.testDy`), module paths (`vllm.entrypoints.openai.api`), bare domains (`github.com`). | Required a real source/config extension plus structural checks. Claims fell 58 -> 21 and 29 -> 7 on the worst offenders. **A dot does not make a path.** |
| Iteration 14 | Measured whether broken-claim ratio alone separates the badge tiers. | It does not, even after the fix: Available 0.260, Functional 0.081, Reusable 0.243 - non-monotonic, and two outliers dominate at n=15. | **Reported rather than buried.** Also surfaced a ground-truth subtlety: `Available` involves *no quality evaluation at all* - it means "archived", so Available vs Functional is partly "what did the authors apply for". Only Functional -> Reusable is a true expert quality ordering. |
| Iteration 15 | Kept the verifier as an *input to the judge* rather than as a standalone predictor. | Verifier still finds real, checkable defects: `zhangxiaosa/LPR` references 12 files that do not exist; `THU-WingTecher/DeepConstr` references 18. | Kept. Whether verified facts beat a raw README *for the model* is the experiment the baseline/solution comparison exists to answer - it is not settled by the raw signal's own correlation. |

| Iteration 16 | Noticed `zhangxiaosa/LPR` reported 12 broken paths despite having 31,020 files. Checked whether the fact sheet was complete. | **4 of 15 fixtures had a truncated file tree** - I stored `paths[:4000]`, so any path beyond the 4000th was reported "not found". The four truncated artifacts were exactly the outliers driving the Iteration 14 result. | Removed the cap. `THU-WingTecher/DeepConstr` fell from 18 broken claims to 4. **My headline negative result had been contaminated by my own bug** - the lesson is that a surprising measurement should be debugged before it is believed. |
| Iteration 17 | Re-measured separation on complete trees. | Available 0.093, Functional 0.081, Reusable 0.215 - still non-monotonic, but now driven by a single genuine outlier (LPR references 12 files that do not exist even against its full tree). | Reframed the verifier honestly: it is a **sparse, high-precision defect detector**, not a tier classifier. It fires rarely; when it fires the defect is real and citable by path. |

## Negative control (isolating the mechanism)

Scoring against ACM badges answers *"does the system agree with experts?"* It does not
isolate *why*. The negative control does: each artifact gets a falsified twin whose
README references files that provably do not exist. Ground truth is exact by
construction, and the injections are seeded, so the control is reproducible.

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 18 | Inject 5 known-false path claims per artifact (75 total) and measure detection. | **84% detected**, 0 false positives. Detection must be 100% - these paths provably do not exist - so the gap was a bug in my checker, not an opinion. | The control earned its keep immediately: it found a defect the badge comparison never could. |
| Iteration 19 | Traced the misses. | Every one came from a fallback rule accepting `scripts/run_x.py` whenever the directory `scripts/` existed. | Narrowed it: a parent directory existing does not satisfy a *file* claim. Detection 84% -> **96%**. "The folder is there" is not "the file is there" - which is the entire point of claim verification. |
| Iteration 20 | Traced the last 3 misses. | All three sat inside a ` ```bash ` block while the two in inline backticks were caught. My code-block regex paired fences positionally, so one unmatched or four-backtick fence upstream misaligned every delimiter after it. | Dropped block-pairing entirely and scanned the whole README - `_is_path` is already strict enough to make that safe. Detection **100% (75/75), 0 false positives**. |

**Result.** The verifier detects 75/75 fabricated claims with zero false positives. The
baseline reads only prose and has no mechanism capable of detecting a fabricated path -
near-blindness is structural, not incidental. This isolates the improvement independently
of how well either system predicts an expert badge.

## Evaluation - where the project changed shape

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 21 | Ran baseline and solution over all 15 artifacts, scored against ACM badge tiers. | Baseline MAE **0.733**; solution MAE **1.000**. The advanced solution was *worse*. | Investigated rather than tuned. Tuning a system against a metric you have not validated is how you get a good number and a wrong conclusion. |
| Iteration 22 | Added a zero-skill control: constant predictors that ignore all input. | `always "Functional"` scores MAE **0.667** - **better than both systems**. The baseline had predicted `Functional` for 14 of 15 artifacts. | **The badge comparison is uninformative.** The baseline only looked better because it mode-collapsed onto the middle class, which MAE rewards on a 3-class ordinal problem. Neither system has demonstrable skill at this task. |
| Iteration 23 | Diagnosed *why* the label does not fit. | The committee badged the curated **Zenodo deposit**; we analyse the living **GitHub mirror**, where README drift is normal and expected. The verifier is correct and is being punished for it - `LPR` genuinely has 15 of 17 README paths missing, the solution correctly downgrades it, and the badge says `Reusable`. | The mechanism is right; the label measures something the evidence cannot see. This was flagged as a risk in Iteration 3 and is now measured rather than suspected. |
| Iteration 24 | **Replaced the primary experiment** with one whose ground truth we author ourselves: falsified READMEs referencing provably absent files. | Removes the confound entirely - we know exactly which claims are false. Same model, same rubric, same input pair; only the evidence differs. | Kept as the primary result. Badge agreement is still reported, with the constant-predictor control beside it, so the negative finding stays visible instead of being quietly dropped. |
| Iteration 25 | First falsified run reported solution 10/15, apparently missing 5. | Every "miss" was already rated `Available` on the clean input - the lowest tier, so it **cannot be downgraded further**. A floor effect, not a failure. | Report raw *and* floor-adjusted rates, with the excluded artifacts named in the output, so the exclusion is auditable rather than a convenient filter. |
| Iteration 26 | Re-ran the identical experiment and got a different number (100% then 90%). | The model is not deterministic even at `temperature: 0`. | **A single run is not a reportable result.** Added repeat trials; the headline figure is a mean with its range. The deterministic components are byte-identical every run, and the contrast between the two is itself part of the finding. |

## Final result

Model `us.amazon.nova-pro-v1:0` (AWS Bedrock), 3 trials, $0.42 total.

| Metric | Baseline | Solution |
|---|---|---|
| Detected falsified README (mean) | **0%** | **97%** |
| Range across trials | 0% - 0% | 90% - 100% |
| Deterministic verifier | - | 75/75 claims, 0 false positives |

Reported alongside the negative finding it replaced: a constant predictor
(`always "Functional"`, MAE 0.667) beats both the baseline (0.733) and the
solution (1.000) on badge agreement. Both results are in the README.

## Extension - from a classifier to a measurement instrument

Once the falsified experiment settled the comparison, the remaining weaknesses were
scope: n=15, one venue, and only one class of defect checked. Each was addressed
with evidence rather than assertion.

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 27 | Searched the literature for whether this problem is actually documented, rather than continuing to assert it. | Over 40% of "functional" artifacts from 2024-25 fail within months; 62.6% break at least once; only 56.4% reachable at published links; link rot averages 9.4%. | **Reframed the negative result.** The badge comparison did not fail because the metric was broken - it was measuring **artifact decay**, which is the documented phenomenon. The gap between a 2024 badge and today's repository *is* the finding. |
| Iteration 28 | Added link-rot checking, since the stated failure mode was "only checks file paths". | 10 dead of 85 URLs (11.8%); 4 of 15 artifacts (27%) carry a dead link. | Kept. That figure sits inside the published 9.4% average and 1.8-29.8% range - **an independent implementation reproducing a known measurement**, which is evidence the tool measures something real. |
| Iteration 29 | Scaled the corpus. The verifier needs no labels and no model, so it can be pointed at everything. | Harvested **398** distinct artifact repositories from Zenodo across venues and "replication/reproduction package" phrasing. | The tool becomes a measurement instrument, not only a classifier. |
| Iteration 30 | The harvester silently returned zero results. | HTTP **400**, not 429 - Zenodo rejects page sizes above 25 as a validation error. My handler printed only the exception type, hiding the status code. | Surface the status code. A "rate limit" that is actually a malformed request wastes the entire debugging budget on backoff tuning. |
| Iteration 31 | Added dependency-pinning analysis - the most-cited cause of decay in the literature. | 53% of the corpus has **no dependency manifest at all**; of those that do, 29% carry a floating requirement. | Kept, and wired into the reviewer report so it names the specific packages that will drift. |
| Iteration 32 | Inspected the pinning output rather than trusting it. | A basename lookup selected `FF_AFL++/frida_mode/ts/package-lock.json` - vendored third-party code six levels deep - as an artifact's own manifest. | Depth-limited selection. The headline numbers moved substantially and more honestly: mean pinned ratio 56% -> 85%, "no manifest" 40% -> 53%. |
| Iteration 33 | Added portability scanning for hard-coded machine-specific values ("incomplete environments" in the literature). | 27% affected. `upbeat` - **badged Functional** - hard-codes 12 paths into `/root/upbeat/`. `inconsistencies-in-tex` reads `/Users/jovyntan/Desktop/2122cmp.csv`. | Kept. An artifact that passed Functional review cannot run outside its author's container. Deliberately narrow patterns: over-reporting trains reviewers to ignore output. |
| Iteration 34 | Wrote 25 regression tests, one per bug this changelog claims to have fixed. | The suite immediately found that `Makefile.am` was not recognised as a path (autotools build files silently skipped), then that the RFC1918 pattern **could never fire** - it demanded three octets after `192.168`, which is already two. | Kept. A changelog entry is a claim; a test is a guarantee. **Both bugs were the same failure class as the truncated file trees and the self-matching scrubber: silent under-reporting.** A check that quietly never fires is indistinguishable from a clean result. |
| Iteration 35 | Built the CLI and the results dashboard, because a measurement nobody can read is not a deliverable. | `artifact-triage owner/repo` runs deterministically in ~5s with no credentials; the dashboard is 8KB with zero external references. | Kept. Every report ends with a *Not checked* section: a reviewer who cannot see a tool's limits cannot responsibly use its output. |

| Iteration 36 | Prevalence sweep over 376 artifacts. | 224/343 (65.3%) carry a broken README claim; 829 of 4,261 claims (19.5%) resolve to nothing. | Kept as a headline finding. Possible only because the verifier needs no labels and no model. |
| Iteration 37 | Two numbers in the first prevalence output were misreported, and I caught them before publishing. | (a) "55% disclose their own badge" counted shields.io CI badges as ACM tier disclosure; the real figure is 6%. (b) The decay test used a median split, but push dates skew hard toward recent, so both halves had medians of 0 and 33 days despite a 5.8-year corpus. | Fixed both. **Letting a computed number speak past what the data supports is the same error the project exists to detect** - convincing rather than correct. |
| Iteration 38 | Re-tested decay with fixed age buckets. | 0.248 (<3mo), 0.224 (3-12mo), 0.249 (1-2yr). Flat, delta +0.001. | **Refines the literature rather than replicating it.** Broken path claims are *not* a decay phenomenon - they are present at publication. Artifacts ship broken, so a reviewer could have caught every one on day one. That is what justifies a mechanical check at review time. |
| Iteration 39 | The trend test reported that flat line as "increasing". | It checked `increasing` before `flat`, so a delta of 0.001 satisfied the first branch. Noise dressed as a finding. | Order the checks so the null case is tested first. |
| Iteration 40 | External validation: do users complain about what the verifier flags? Falsification condition written down first. | **Null.** Flagged 37.1% vs clean 37.5% - indistinguishable. | Reported as a null. The reason is the more interesting result: only 36% of these repositories have *any* issues. Research artifacts are published and never exercised, so silence is not evidence they work, and user complaints are a weak instrument either way. |

| Iteration 41 | Added "did you mean" suggestions - a report that only says *wrong* leaves the author hunting. | Verified on near-misses: `config/base.yaml` -> `configs/base.yaml`, `run_experiments.sh` -> `run_experiment.sh`, with an extension guard so a `.py` claim is never answered by a `.md` file. | Kept. Building it surfaced the project's strongest example: `zhangxiaosa/LPR` returns **no** suggestion for any of its 15 broken paths, because the repository contains **zero Python files across 31,020 paths**. Its top level is `precomputed_results/` (29,056 files), `benchmark_suites/`, the paper PDF and the README. The artifact ships its results and benchmark inputs but **not the tool its README documents** - and it is badged `Reusable`. |
| Iteration 42 | Ran every documented `make` target to check it worked, rather than assuming. | **`make corpus` pointed at `artifact_triage.corpus.build` - a module that was never written.** `make eval` pointed at another that did not exist. `make repro`, the single command a judge runs, failed immediately. | **The project's own repository contained exactly the defect the project detects: a documented command that does not exist.** Fixed both, and added `scripts/verify_targets.py` so the claim is checkable rather than asserted - it runs every credential-free target and fails loudly if any does not. All 8 now pass. |
| Iteration 43 | Moved the zero-skill control into `make eval` itself. | The comparison now prints the constant-predictor baselines every run and states plainly when a real system fails to beat them. | The honest caveat is enforced by the tooling instead of depending on me remembering to write it down. A result that flatters itself now has to get past its own reporting code. |

| Iteration 44 | Ran the tool on its own repository (`make selfcheck`). | Found two real omissions - **no LICENSE, no CI** - and one real false-positive class. | Added MIT licence and a GitHub Actions workflow that asserts the negative control still reports 75/75 with 0 false positives. The tool flagged its own author for the category of omission it flags in others. |
| Iteration 45 | Investigated the 6 "broken paths" it reported in our own README. | All six are **quotations from other artifacts** - `scripts/run_lpr.py` is LPR's, shown as example output. Meta-documentation: a README discussing another project's paths. No artifact in the 376-repo corpus does this, so only self-application could surface it. | Added `.artifact-triage-ignore` for author-declared exceptions, with one rule: **the report always states how many were applied.** A silent suppression would make the tool's own output unfalsifiable. |
| Iteration 46 | Checked whether the human-escalation gate had ever actually fired. | **0 out of 15.** Worse, self-reported confidence took only three values and was **anti-calibrated**: mean 0.700 when the answer was right, 0.750 when wrong. The gate was wired to the one signal in the system carrying no information. | **Replaced it with evidence-based rules.** Escalate when the README makes no checkable claims, when the verdict contradicts verified evidence, or when the environment cannot be recreated. Now fires 5/15. **Correction (Iteration 71):** an earlier version of this entry claimed LPR escalates for being rated `Reusable` over 15 of 17 missing paths. That was wrong - I had tested the rule by passing the *true badge* instead of the model's prediction. The model predicts `Available` for LPR, and the contradiction rule has never fired in production. |
| Iteration 47 | Read the report the CLI actually produced, rather than trusting the diff. | It listed five missing paths and then printed **"All referenced paths were found."** My edit had put the new branch between an `if` and its `elif`. | Fixed, and pinned with a regression test. **A report contradicted by its own evidence is precisely the defect this project detects** - and I shipped one. It survived a passing test suite because no test read the rendered output. |

| Iteration 48 | Ran a harder control: mutate paths that genuinely exist into near-misses (`run.py` -> `run_v2.py`, `config/` -> `configs/`) rather than inventing fake ones. | 40/44 detected (91%), and the suggester named the correct original file for 32/44 (73%). | Kept. Closer to how references actually go stale, and the only variant where "did you mean" can be scored, because the right file still exists. |
| Iteration 49 | Ablated the strict path extractor against the naive "any dotted token" rule it replaced. | **Identical recall** (75/75 both). On unmodified READMEs the naive rule flags **195** paths as broken versus **35** - the extras being `zenodo.org`, `20.04`, `3.7.4`, `FF_DRIVER_NAME.final.bc`. | The complexity is justified. A checker reporting 195 defects where 35 exist gets switched off, so suppression is the job, not polish. |
| Iteration 50 | Noticed the solution agent only ever received **path** evidence, though four more checks had been built. | It was reasoning with a fifth of the evidence the system already had, for free, none of it hallucinable. | `solution/evidence.py` assembles all five under two rules: report facts not verdicts (pre-judging would make the model's answer a restatement of ours), and state absence explicitly (silence is ambiguous between "clean" and "not checked"). |
| Iteration 51 | Realised the results files had been produced by code that had since changed. | Nothing recorded which code produced which number. The README's headline came from an implementation no longer in the repository. | Added `common/provenance.py`: every result carries the commit and a content fingerprint of the modules that determine it, and `check_claims.py` reports staleness. **This is the project's own subject applied to itself** - a claim that silently stops matching the thing it describes. |
| Iteration 52 | Pinned the fairness invariant the entire comparison rests on. | Nothing had ever checked that the baseline receives no verified evidence. | Three tests: the baseline prompt contains no evidence markers, both systems see the identical README, and both reference one rubric object. If that stops holding the measured improvement means nothing. |

| Iteration 53 | Re-ran the experiment with the enriched evidence block. | Solution detection went **97% (90-100%) -> 100% (100-100%)** across three trials; baseline stayed at 0%. Solution MAE improved 1.000 -> 0.700 and escalation now fires 33%. | Kept. The variance disappeared: showing the model everything the deterministic checks already knew removed the cases it was previously getting wrong by chance. |
| Iteration 54 | Checked the spend tracker against the runs actually performed. | It reported **$0.49 against a true $1.12** - it summed the cost fields in `results/*.json`, which measures the cost of the *current* results. Every re-run overwrote its file and the previous run's cost silently vanished. | Replaced with an append-only ledger: a run can add to the total, nothing can subtract from it. **Fifth under-reporting bug in this project, and the only one with consequences** - under-reporting against a hard budget is how you reach the ceiling without warning. |

| Iteration 55 | Compared my spend ledger against AWS Cost Anomaly Detection, which the project owner surfaced. | Ledger said **$1.12**; AWS billed **$2.39**. Wrong by **2.2x**, in the direction that reaches a hard budget without warning. | **Sixth under-reporting bug, and the second with consequences.** I had instrumented the success path only: retries (up to 5 per call), model-access probes, preflight and CLI runs all consumed billed tokens invisibly. A retry is a call you were charged for. |
| Iteration 56 | Fixed the instrumentation and added enforcement. | Every call is now metered at the moment it is made, including failed attempts, which are recorded with a conservative cost because an unknown charge is not a free one. A hard `budget_check` raises rather than warns. | The deeper lesson is the project's own: **my ledger was internally consistent, passed its own tests, and was confidently wrong.** It only surfaced because an external source of truth existed. A self-reported metric with no external check is precisely what this project argues you cannot trust - and I built one and believed it. |

| Iteration 57 | Ran a clean-clone test - which the README had claimed reproducibility for days without my ever doing. | A judge would download **551 MB and wait 35 seconds** before touching anything. The bulk was 336 MB of regenerable file-tree API responses. | Dropped the regenerable caches: **551 MB / 35s -> 37 MB / 3.2s** shallow. Verified by *deleting them from disk* and re-running - all ten credential-free targets still pass. A `.gitignore` entry proves nothing until you remove the file and try. |
| Iteration 58 | Audited for **false negatives** for the first time. Every check had been examined for false positives; nothing had asked what we were failing to see. | 64 backtick-quoted **directory references** were silently skipped. "See `reproduction/`" promises that directory exists, and `verify()` already knew how to check directories - the extractor never passed them through. | Added, conservatively: only an explicit trailing slash counts, or `CC/CXX` gets swept in. Claims per artifact up 10-25%; `nhuhuan/sba` went from *zero* checkable claims to one, which is broken. |
| Iteration 59 | Re-harvested the corpus stratified by publication year, to fix the recency skew the datasheet had to declare. | Corpus 398 -> **769** repositories spanning 2018-2026; 732 profiled. The oldest age bucket went from **n=8 to n=173**, median 1,423 days. | The decay test is now properly powered - and the null **survived**: 0.256 / 0.232 / 0.310 / 0.251, delta -0.005 across four years. |
| Iteration 60 | Found the staleness detector had a blind spot. | Changing the extractor correctly invalidated `baseline` and `solution` but left `falsified_run` reporting *current*, because `fetch.py` was missing from its fingerprint. | Tracked everywhere, with a test asserting every result kind fingerprints the extractor. **A staleness detector with a blind spot is worse than none: it certifies the thing it cannot see.** |
| Iteration 61 | `prevalence.py` printed 63.8% while `check_claims.py` printed 63.7%. | Two code paths formatting one number - the first from the raw ratio, the second after rounding to 4dp and storing. I had already "corrected" the README in the wrong direction because of it. | The producer now writes a `display` block and the checker reads it. Same string by construction rather than three independent roundings agreeing. |

## Precision audit - the weakness the negative control could not see

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 62 | Hand-audited a random sample of reported-broken claims against the real repositories. Until now nothing had checked whether real-corpus findings were *correct* - only that injected ones were caught. | **Precision was 78%, not the ~100% the negative control implied.** 22% of findings were noise across three classes: runtime output directories (14%), case-only mismatches (4%), explicit placeholders (4%). | **The negative control could never have found this.** It tests claims *we* injected, which are perfect by construction. A control validates the mechanism; only auditing real output validates the result. |
| Iteration 63 | Fixed all three classes. | `path/to/x.csv` and `TMP_DIR/y` are placeholders, not promises. `out/`, `logs/`, `results/` are directories a tool *creates*, not files it ships - and that class was introduced by my own directory-reference feature two iterations earlier. Precision **78% -> 100%** on the labelled corpus. | Verified the filters do not over-correct: all 10 suppressed claims were hand-checked and none was a real defect. A filter that hides findings is worse than the false positives it removes. |
| Iteration 64 | Case-only mismatches needed care: `README.MD` when the file is `README.md`. | It works on macOS and Windows and **fails on Linux**. Calling it "missing" is wrong; dropping it silently loses a real portability defect. | Reported as its own finding type. Prevalence moved 63.8% -> 62.5% and claims 6,687 -> 6,510 as the noise came out; the decay null survived unchanged. |

| Iteration 65 | Tested the objection **"your baseline is a strawman"** - it was never told to check consistency. Ran a baseline explicitly instructed to hunt for internal contradictions. | **0/13 (0%).** Identical to the plain baseline. | The limitation is structural, not a prompting artefact I engineered. Reading prose cannot find a fabricated path however hard you ask it to. |
| Iteration 66 | Tested the objection **"the solution reads the README, not the evidence"** with a placebo: the falsified README paired with an evidence block of identical structure whose content claims every path resolves. | **Detection collapsed 93% -> 0/11 (0%).** | **This could have destroyed the central claim and instead confirmed it.** Identical model, rubric, README and block structure; only the evidence *content* inverted. When the facts say clean, the solution says clean - while looking at a README it has every textual cue to distrust. A causal result, not a correlation. |

| Iteration 67 | Tested whether the result generalises beyond one model, re-running on Llama 3.3 70B. | **9% detection** - and 7 of 15 answers recorded as unparseable. | I had the story written: *the improvement is model-dependent*. Then read the raw output instead of accepting a plausible negative. |
| Iteration 68 | Inspected what Llama actually returned. | It **echoes the JSON schema before its answer**. My parser used a greedy `{.*}`, spanning from the first brace to the last, so every such response became invalid JSON. The model had answered correctly every time. | Replaced with a balanced-brace scanner that finds the first object carrying an actual `tier` value. Llama: **9% -> 100%**, unparseable 7/15 -> 0/15. |
| | | | **Seventh measurement bug, and the most dangerous.** It would have under-reported a *different system* - the shape that produces confident, publishable, wrong comparative claims. "We evaluated two models and it only worked on one" is exactly the sentence that gets believed. |
| Iteration 69 | Reported the corrected generalisation result. | Nova Pro: baseline 0%, solution 93%. Llama 3.3 70B: baseline 0%, solution 100%. Verifier 100% on both. | The improvement transfers across unrelated model families, and the baseline's blindness is structural on both - which is what the causal claim predicts. |

| Iteration 70 | Addressed the strongest remaining methodological objection: **the floor effect**. Tier downgrade cannot measure an artifact already rated `Available`, so those are excluded - which a reviewer could fairly call cherry-picking. | Found a metric with no floor in data already recorded: **does the system's reasoning mention the fabricated absence at all?** Every artifact is eligible. | **Baseline 0/60 across two model families. Solution 59/60.** Including 10/10 on the artifacts the downgrade metric had to drop. Backfilled onto the recorded runs, so it cost nothing. |

## Literature grounding and prior-art disclosure

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Iteration 71 | Searched the scholarly literature (OpenAlex/Semantic Scholar, ~250M records) to test whether the problem is real and whether the tool already exists. | **It already exists.** [READU](https://arxiv.org/abs/2607.15780) (Baek, Krampf & Pradel, 2026) detects README-vs-repository inconsistencies at 75% precision over 6,000 commits from Linux and Spring Boot - **and repairs them**, with 44 confirmed real-world fixes. [Tan, Wagner & Treude](https://doi.org/10.1007/s10664-023-10397-6) (EMSE 2023) detect outdated code-element references across 3,000+ projects. | **Disclosed prominently rather than buried**, per ground rule 2. READU is more capable than this project on the shared task. The mechanism here is a competent re-implementation of a published idea, and saying otherwise would be the exact dishonesty this project is about. |
| Iteration 72 | Determined what remains after removing what prior work established. | Neither measures **prevalence in research artifacts** at population scale; neither tests **whether the defect accumulates with age**; neither compares against **an LLM reading the README alone**. Arvan et al. ([EMNLP 2022](https://doi.org/10.18653/v1/2022.emnlp-main.150)) independently recommend evaluating artifacts "at the time of publication" - the moment this tool targets. Olszewski et al. ([CCS 2023](https://doi.org/10.1145/3576915.3623130)) find AECs have not shifted artifact availability across ~750 papers, locating the bottleneck in reviewer capacity. | The contribution is **measurement and framing, not the detector**. Stated that way in the README rather than implied otherwise. |
| Iteration 73 | Checked whether our precision figure is comparable to READU's. | It is not. Ours is 100% on a **hand-audited sample of 37 findings** from our own labelled corpus; READU's 75% is a systematic evaluation on a larger, harder, general-OSS corpus. | Recorded the non-comparability explicitly. Presenting 100% against their 75% would be a true number used to mislead. |
| Iteration 74 | Acted on the literature's actual finding. If the bottleneck is reviewer capacity (Olszewski, CCS 2023) and the moment is publication time (Arvan, EMNLP 2022), then a better detector is not the gap - **meeting the reviewer at the decision they must make** is. Retrieved the ACM `Functional` criteria verbatim. | ACM names four qualities: **documented, consistent, complete, exercisable**. Our core check maps precisely onto `Complete` - a documented file that does not exist *is* evidence of incompleteness. `Documented` and `Exercisable` get partial, necessary-condition evidence. And `Consistent` - does the artifact relate to the paper's claims - is **definitionally unmechanisable**. | Built `solution/criteria.py`: the report now answers the reviewer's own checklist, quoting each criterion verbatim, and marks `Consistent` as always-human **by construction rather than by a confidence threshold**. A regression test asserts it can never be marked machine-checkable. The ACM policy page itself returned HTTP 403, so the text came from a conference reproduction - disclosed in `RELATED_WORK.md`. |
| Iteration 75 | The provenance checker reported three results stale, including the headline. Rather than trusting or re-running blindly, **diffed the evidence blocks under old and current code, for free.** | 7 of 15 fixtures changed. Every path the precision fix removed was a **false positive** - so the old evidence blocks were *stronger* than what current code produces. The reported 93% detection was measured against inflated evidence, biased in the direction that flatters the result. | Re-ran all 3 trials on current code: **detection 93% -> 96% (89-100%)**, baseline unchanged at 0%, and the floor-free metric went 59/60 -> **60/60**. It came out *higher*. It did not have to, and the reason to re-run was that the bias ran toward the claim. A staleness warning on the one result the whole project rests on is not a formality. |
| Iteration 76 | Noticed the spend report claimed "46 tests". | Four documents asserted **four different counts** - 46, 29, 29, 18 - while the suite stood at 75. Each was correct when written; none when read. Same failure class as the truncated file trees: a claim that silently stops being true. | `_n_tests()` now derives the count, and a test scans the present-tense docs and fails on any drift. It **caught itself on its first run** - adding the two guard tests moved the count to 77. CHANGELOG is exempt: its entries are historical, and rewriting them would falsify a log. |
| Iteration 77 | With the headline re-measured, checked whether *other* documented numbers had drifted the same way. | They had. The negative-result table read **MAE 0.733 / 1.000**; a re-run put both systems at **0.800**. The video script quoted the same stale pair aloud. And `check_claims` was **silently skipping** the floor-free claim - it read a top-level key that the aggregate never wrote, so `59/60` had never once been verified. | Registered the comparison MAEs, the constant-control MAE, and the floor-free metric (derived from per-artifact records across **both** model runs) as checked claims, in the README *and* the video script. **18 documented numbers now verified, and every result is provenance-current.** A claim checker that quietly omits a claim is the same defect it exists to catch. |
| Iteration 78 | Re-ran `baseline` and `solution` so nothing was left stale. | The negative result **holds**: a constant `Functional` predictor (MAE 0.667) still beats baseline and solution (0.800 each). The baseline answers `Functional` for 13 of 15 artifacts. | Kept, corrected, and now machine-checked. The finding that the project's original evaluation does not work is the most load-bearing honest claim in the write-up; letting its numbers rot would undo the point of keeping it. |
| Iteration 79 | Checked whether the new criteria block contradicts anything already in the report. | It did. `Decision.explain()` said **"handled automatically - evidence was sufficient"**, and the CLI printed nothing about review when no rule fired - while the criteria section two blocks later states that `Consistent` can *never* be settled by a machine. The report claimed autonomy in one section and disclaimed it in the next. | Separated **structural** human need (always true: rule on `Consistent`, run the artifact) from **additional** evidence-triggered escalation, so the escalation signal stays meaningful without the report overclaiming. This is the same defect as the earlier "5 missing paths / all paths were found" bug, and the same one the project detects in other people's READMEs. |
| Iteration 80 | Hunted the *class* of bug that had already produced two findings - code that never executes - instead of waiting to trip over another. Wrote a tracer that runs the test suite plus a full CLI report and lists every function never entered. | **`budget_check` was among them.** Reading it exposed the real defect: it is called from `client()`, which runs **once per run**, while `ask()` meters every call and re-checks nothing. A run starting at $4.95 passed the check and could then make hundreds of billed calls unwatched. The ceiling was enforced at *run* granularity, not *spend* granularity - no ceiling at all for any run big enough to matter. | Enforced per call inside `_meter`, with four tests that actually enter the guard. Safe to raise mid-run precisely because iteration 76 made every long experiment checkpoint its completed work first. (I also suspected the $1/$2/$3/$4/$5 alerts were dead - **they were not**, they are wired in `ledger.record` and tested. Only `remaining()` is unused.) |
| Iteration 81 | The same sweep flagged `to_dict` as unused on four report dataclasses. | Not stray code - **a serialisation layer built for an output mode that was never implemented.** The CLI emitted markdown only, while the entire thesis is reviewer *capacity*: a chair triaging a venue needs a sortable record per artifact, not prose read one repository at a time. | Added `--json`, which makes the existing serialisers live and closes a real product gap. The dead code was pointing at the missing feature rather than at itself. |
| Iteration 82 | The budget fix made the provenance checker mark `baseline` and `solution` stale - for a change that cannot alter a single token of model output. | The fingerprint hashes **whole files**, and `llm.py` held two unrelated concerns: the inference path, which genuinely changes results, and budget enforcement, which cannot. Every budget fix would press for a paid re-run that could not produce a different answer. **A staleness detector that cries wolf trains you to ignore it** - worse than the wasted spend. | Split budget policy into `common/budget.py`. Not a looser fingerprint: a detector with a blind spot is worse than none, as this project learned when `fetch.py` was missing from the influencer list. File-level hashing becomes *accurate* when files have coherent single responsibilities. A test asserts `budget.py` is never an influencer. |
| Iteration 83 | Re-running `baseline` on unchanged code returned **MAE 0.733**, where the previous run gave **0.800**. | The model is not deterministic at temperature 0. `falsified_run.py` already knew this - it runs 3 trials and reports a range, with a comment saying "a single run is not a reportable number" - but that standard had **never been applied to the badge-agreement metric**, which was published to three decimals with no spread. The project held one experiment to a stricter standard than the other. | `make eval` now appends every scoring to `results/comparison_history.jsonl` so the spread accumulates instead of being overwritten, and the README reports the observed movement. **The conclusion is unaffected:** the best control needs no model and no input, so its 0.667 is exact, and every observed value sits above it. |
| Iteration 84 | Checked the CLI's exit codes, since a judge will run it. | Errors exit 1 correctly - but an artifact with 15 missing documented paths also exited **0**. Arvan et al. recommend evaluating artifacts "at the time of publication"; in practice that is a check in the author's own CI, and **CI gates on exit codes**. Without one, the moment the literature identifies was unreachable. | Added opt-in `--fail-on-findings` (exit 2), so plain reporting still exits 0. Wired into this repository's own CI, running the tool against itself - **it passes**. `Consistent` alone never fails the gate: it is un-checkable for every artifact, so failing on it would fire always, which is the same as never. |
| Iteration 85 | Attacked the **scrubber**, since leakage would void every comparison: if the README discloses its badge, the model reads the answer instead of judging the work. | `artefacts?\|artifacts?\s*(?:evaluated\|available)...` is an **ungrouped alternation** - it reads as *(artefacts?)* OR *(artifacts?...)*, so the British spelling matched the bare word alone. **"Artefacts Evaluated - Reusable" became "[REDACTED] Evaluated - Reusable"**: label removed, tier left in plain sight, the exact failure that file's own badge-image comment warns about. It also over-redacted every innocent "artefact". The separator class missed `(Reusable)`, `/ Functional` and `_reusable`. | Fixed, with 10 leak phrasings and 6 innocent sentences asserted in both directions. **I reintroduced the identical precedence bug in the very next pattern while fixing it** - noted in the code, because a regex precedence error is invisible until something tests the branch. **Verified against the corpus: no fixture ever leaked, and the fix is a byte-for-byte no-op on all 15, so no recorded result was contaminated.** Reported as a latent hole, not a live one. |
| Iteration 86 | Checked whether `scrub.py` was covered by the provenance fingerprint. | **It appeared in no influencer list.** The single strongest lever on what a model can conclude was invisible to the staleness detector - the same blind spot `fetch.py` had, which the changelog already argues is worse than no detector at all. | Not fixed by adding it to every list: `scrub.py` does not influence a result *directly* - `baseline` and `solution` read committed fixtures, so **the fixtures are the interface**. Doing that would mark results stale for a change provably unable to alter them, re-introducing the cry-wolf failure iteration 82 removed. Instead the **corpus is fingerprinted separately**, over fixture bytes plus the modules that produce them. Results recorded before the check report "predates this check" rather than a fabricated failure. |
| Iteration 87 | Stress-tested the path extractor against adversarial inputs, since precision was only ever hand-audited on 37 findings. | **External URLs were counted as claimed repository files.** The token pattern cannot contain `:`, so `https://github.com/other/repo/blob/main/tool.py` degraded to `//github.com/.../tool.py` and survived as a path that by construction can never exist here. Measured on the sweep: **126 of 1,190 reported-broken paths (10.6%)** were links to other projects - `github.com/...`, `conda.io/docs/...`, `pandoc.org/MANUAL.html`. The datasheet warned this *could* happen; nobody had measured it, and an unquantified caveat beside a headline percentage is not a disclosure. | URLs stripped before extraction. **Headline corrected: prevalence 62.5% -> 56.7%, broken-claim rate 21.4% -> 18.6%.** The finding survives - a majority of research artifacts still carry a broken claim - but it is smaller than published, and it was published too high. |
| Iteration 88 | Re-ran the sweep after the fix and the numbers came back **byte-identical**. | The cache stored `readme_referenced_paths` - a **derived** value - so the fix could not reach any cached artifact. **And the provenance stamp said "current":** it hashed the new extractor while the numbers came from values the old one produced. A staleness detector certifying stale numbers is precisely the failure this project argues is worse than having none. | The cache now stores only what was *fetched*; everything derived is recomputed on load. Recomputation reads the **raw** README, not the stored copy, which is truncated at 20,000 characters - **45 cached entries sit exactly at that cap**, so re-extracting from the stored text would have silently undercounted. That is why total claims went **up** (6,510 -> 6,740) while broken went **down**: two corrections in opposite directions. |
| Iteration 89 | Applied the same principle to the labelled corpus and rebuilt it. | Only **1 of 15** fixtures changed, dropping a single Anaconda installer URL. The paid experiments could barely move. | Re-ran them anyway. "Provably small" is an argument, and this project's position is that arguments do not replace measurement - the same call on the morning's re-run moved the headline further than expected. |
| Iteration 90 | Re-ran everything on the corrected corpus. | **Detection went 96% -> 100% (no variance across 3 trials)**, and the floor-free metric moved the *other* way, 60/60 -> **59/60**. Then the anti-cry-wolf mechanism cried wolf: rebuilding `data/fixtures` marked **`prevalence`** stale for data prevalence never reads. **The fix for false staleness, itself producing false staleness.** | Scoped the corpus fingerprint to the kinds that actually read the fixtures. The rule that keeps recurring: a fingerprint must cover **exactly** what a result consumed - too little certifies stale numbers, too much trains you to ignore the alarm. |
| Iteration 91 | Made `check_claims` print **where** each documented number matched. | Two "verified" numbers verified nothing. `trial count` matched the bare string **"3"** on 56 lines. `solution MAE` matched **"0.700"** on the *anti-calibrated confidence* sentence rather than the MAE table - a coincidence, reported as a pass. **An unaudited green check is the exact thing this project argues against**, and there were two of them in its own checker. | Anchored every literal to its table row, and the checker now flags any literal matching more than 8 lines as **TOO LOOSE** rather than passing it. 18 numbers verified, 0 loose. |
| Iteration 92 | Investigated junk slugs seen scrolling past during the sweep (`user-attachments/assets`, `dgalan7/minif.`). | The GitHub URL pattern allows `.` inside a repository name, so a link written in prose - *"see https://github.com/owner/repo."* - captured **the sentence's full stop**. GitHub forbids a trailing dot in a repo name, so those slugs could only 404. **10 of 769 were affected, and all ten were confirmed real repositories via the API** - legitimate artifacts silently dropped from the measured population, which quietly biases a prevalence figure. Three more were github.com paths that are not repositories at all. | Fixed, migrated with a reviewable script that prints every change, and re-measured: **742 artifacts profiled (was 732), prevalence 56.3%, 6,840 claims**. A test caught an **ordering bug in my own fix** - `.git` must be stripped *after* the full stop, or `repo.git.` survives as `repo.git`. Reading the code did not catch it; the test did. |
| Iteration 93 | While recovering those repos, ran `make discover` - and it **overwrote the corpus**. | The corpus reached 769 repositories through a **stratified** harvest across publication years. A plain run finds 398 and replaced the larger file with the smaller one, halving the measured population **without a word**. Caught only because a backup existed. | `make discover` now refuses to shrink an existing corpus, naming both counts and offering `ARGS=--stratified` to extend or `--force` to override. A build target that destroys data silently is a defect whether or not anyone has lost anything to it yet - and this time, someone had. |
| Iteration 94 | The rubric puts reproducibility behind a **pass/fail gate** - a project that cannot be run is disqualified before any scoring - so I stopped asserting it and tested it: fresh clone of the **published** repo, fresh virtualenv, no credentials, no cached state. | **All ten credential-free targets pass**, and the installed `artifact-triage` entry point produces a report. One documented claim had drifted: the guide promised a **37 MB** clone; the real one is **46 MB**, because cache files were added and the figure was never revisited. | Corrected, and pinned by a test that measures the tracked tree instead of trusting a remembered number. A stale size claim in a reproduction guide is the same defect this project detects in other people's READMEs - here, in its own. Trajectories were also stale, and were regenerated (secret redaction fired: 3 AWS secrets, 43 env assignments; independently re-scanned for credential shapes, none found). |

## Experiments removed

- **Shallow cloning** (Iteration 9). Filled the disk to 100% on a 15-artifact corpus. Taught me that repo *size at HEAD* and *clone size* differ by more than an order of magnitude, and that the fact I needed (path existence) never justified transferring bytes at all.
- **GitHub search for missing repos** (Iterations 4-5). Recovered 20 artifacts, but a measured false-positive rate that two rounds of hardening could not eliminate. Cutting it traded corpus size for label integrity. The lesson is on-theme: fuzzy matching produced *confident, plausible, wrong* answers - precisely the failure mode this project exists to detect.
- **Downloading Zenodo deposits** (Iteration 1). Correct in principle - the deposit is the badged unit - but 326 GB makes it unusable, and it would have pushed our own reproduction time from seconds to hours. Analysing the GitHub mirror trades a little fidelity for a pipeline judges can actually re-run.

## Known threats to validity

- The badge was awarded to the Zenodo deposit; we analyse the GitHub mirror, which may have drifted since. Mitigated by pinning to a commit and recording it.
- 12 of 43 artifacts never resolved to Zenodo. Corpus is therefore a subset, not the full venue.
